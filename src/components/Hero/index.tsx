import { Container } from '../../global'
import { Form, FormContainer, HeroTitle } from './styles'

const Hero = () => (
  <Form>
    <Container>
      <FormContainer>
        <HeroTitle>
          As melhores vagas para tecnologia, design e artes visuais.
        </HeroTitle>
      </FormContainer>
    </Container>
  </Form>
)

export default Hero
